# AGENTS.md

> 本文件是 AI 编码 Agent 在此仓库工作的入口文件。
> 请保持在 100 行以内。指向详细文档，而非内联所有内容。

## 项目概览

<!-- TODO: 2-3 句话描述项目做什么 -->

**技术栈**: <!-- 例如：TypeScript + React + Node.js + PostgreSQL -->
**包管理器**: <!-- 例如：pnpm（不是 npm） -->

## 常用命令

```bash
# 安装依赖
TODO_INSTALL_CMD

# 启动开发服务器
TODO_DEV_CMD

# 运行测试
TODO_TEST_CMD

# 运行 Lint
TODO_LINT_CMD

# 生产构建
TODO_BUILD_CMD
```

## 架构

<!-- TODO: 简要的 3-5 行架构概述 -->
<!-- 详细架构文档应放在单独文件中 -->

See: `docs/architecture.md`

### 依赖方向（由 CI 强制执行）

<!-- 在下方定义你的项目分层。示例： -->
<!-- Types → Config → Repository → Service → Runtime → UI -->
```
TODO_LAYER_1 → TODO_LAYER_2 → TODO_LAYER_3 → TODO_LAYER_4
```

反方向的导入将导致构建失败。

## 目录结构

<!-- 根据你的项目自定义以下目录结构。 -->
<!-- 每个目录应标注它可以从哪些层导入。 -->
```
src/
├── [layer-1]/    # [描述]（不从其他 src/ 目录导入）
├── [layer-2]/    # [描述]（导入: layer-1）
├── [layer-3]/    # [描述]（导入: layer-1, layer-2）
├── [layer-4]/    # [描述]（导入: 以上所有）
└── ...
```

### 如何自定义此模板

1. 将所有 `TODO_*` 占位符替换为你的实际命令和值
2. 根据你的架构定义自己的依赖分层
3. 填写与项目匹配的目录结构
4. 在约定章节填入你的 5 条机械化强制规则
5. 完成后删除这些说明和所有 `<!-- -->` 注释

## 约定

<!-- TODO: 链接到详细的约定文档 -->

See: `docs/conventions.md`

### 前 5 条规则（机械化强制执行）

1. <!-- 例如：所有 API 响应使用 `ResponseWrapper<T>` 类型 -->
2. <!-- 例如：日期以 UTC 存储，仅在 UI 层转换 -->
3. <!-- 例如：repositories/ 之外不允许直接数据库查询 -->
4. <!-- 例如：所有新端点需要集成测试 -->
5. <!-- 例如：错误信息必须包含 errors.ts 中的错误代码 -->

## 测试

<!-- TODO: 简要的测试策略概述 -->

See: `docs/testing.md`

- 单元测试: `src/**/*.test.ts`
- 集成测试: `tests/integration/`
- E2E 测试: `tests/e2e/`（需先运行 `docker compose up`）

## 常见错误

| 错误 | 为何有问题 | 修复方法 |
|------|-----------|---------|
| <!-- 例如：在 UI 层导入 service --> | <!-- 违反依赖方向 --> | <!-- 将共享逻辑移至 types/ --> |
| <!-- 例如：使用 `any` 类型 --> | <!-- 绕过类型安全 --> | <!-- 在 types/ 中定义正确的类型 --> |
| <!-- 例如：集成测试中 mock 数据库 --> | <!-- 隐藏真实数据库行为 --> | <!-- 使用带重置的测试数据库 --> |

## 遇到困难时

1. 运行 `TODO_TEST_CMD` 检查你的改动是否破坏了什么
2. 查看 `docs/` 中相关领域的详细文档
3. 参考最近的 PR 寻找类似变更的示例
4. 如果测试不稳定，查看 `docs/known-issues.md`
